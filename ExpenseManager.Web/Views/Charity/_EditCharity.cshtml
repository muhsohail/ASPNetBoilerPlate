@model ExpenseManager.Charity.Dto.UpdateCharityDto

@using ExpenseManager.Web.Models.Common.Modals
@using Abp.Web.Mvc.Extensions

@{
    Layout = null;
}

@{
    ViewBag.ActiveMenu = PageNames.Users; //The menu item will be active for this page.
}
@section scripts
{
    @Html.IncludeScript("~/Views/Charity/_EditExpense.js")
}
@Html.Partial("~/Views/Shared/Modals/_ModalHeader.cshtml", new ModalHeaderViewModel(L("EditCharity")))

<div class="card">
    <div class="body">
        @using (Html.BeginForm("Update", "Charity", FormMethod.Post))
        {

            @Html.HiddenFor(model => model.Id)

            @*<h4 class="text-center">@L("Update")</h4>*@

            @Html.AntiForgeryToken()

            if (@ViewBag.ErrorMessage != null)
            {
                <div class="alert alert-danger">
                    <i class="fa fa-warning"></i> @ViewBag.ErrorMessage
                </div>
            }


            <div class="form-group form-float">
                <div class="form-line">
                    <input type="text" class="form-control" name="Amount" value="@Model.Amount">
                </div>
            </div>

            <div class="form-group form-float">
                <div class="form-line">
                    <input type="Date" class="form-control" name="DateSpent" value="@Model.DateSpent.Value.ToString("yyyy-MM-dd")">
                </div>
            </div>



            <div class="form-group form-float">
                <div class="form-line">
                    <input type="text" class="form-control" name="Purpose" value="@Model.Purpose">
                </div>
            </div>

            <div class="row">
                <div class="col-xs-12">
                    <a href="@Url.Action("Index", "Charity")" cl class="btn btn-default waves-effect">@L("Back")</a>
                    <button id="RegisterButton" class="btn bg-pink waves-effect" type="submit">@L("Update")</button>
                </div>
            </div>
        }
    </div>
</div>